/**
 * The key to sign any new authentication token.
 * @type {string} The unique key. Changing this key, leads to the deletion of all active session, as no key would be recognized anymore.
 */
const authKey = "efüwkeüfküwekf2934iur03u4r9u3ßfjwe"

module.exports = {
    authKey
}